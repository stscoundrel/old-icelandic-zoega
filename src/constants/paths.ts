import path from 'path';

export const ZOEGA_JSON = path.join(`${__dirname}/../../json/zoega.json`);
export const NO_MARKUP_JSON = path.join(`${__dirname}/../../json/zoega-no-markup.json`);

export default {
  ZOEGA_JSON,
  NO_MARKUP_JSON,
};
