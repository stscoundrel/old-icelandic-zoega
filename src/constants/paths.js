const path = require('path')

const ZOEGA_JSON = path.join(`${__dirname}/../../json/zoega.json`)
const NO_MARKUP_JSON = path.join(`${__dirname}/../../json/zoega-no-markup.json`)

module.exports = {
  ZOEGA_JSON,
  NO_MARKUP_JSON,
}
